name: mastertrader
description: "App TerlineT Master Trader com Bee Agent"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  solana: ^0.30.0
  url_launcher: ^6.2.1
  just_audio: ^0.9.36
  speech_to_text: ^6.6.0
  permission_handler: ^11.0.1
  image_picker: ^1.0.4
  video_player: ^2.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/trader.jpg
    - assets/Trader.mp4
    - assets/beeagent.mp3
